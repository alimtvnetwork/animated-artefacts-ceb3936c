{
  "templateName": "AdvanceStepSlide",
  "depends": {
    "color": "../themes/noir-gold/colors.json",
    "theme": "../themes/noir-gold/themes.json"
  },
  "fields": {
    "eyebrow": { "type": "string", "required": false },
    "title":   { "type": "string", "required": true },
    "steps":   { "type": "Step[]", "required": true, "description": "Vertical reel of full-viewport step frames. Each step has label/title/description; capsule optional." }
  },
  "defaults": {
    "transition": "FadeIn",
    "textAnimation": "FadeIn"
  }
}
