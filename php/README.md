# PHP Backend (placeholder)

This folder reserves the future PHP backend per the architecture spec.
See `/spec/architecture/architecture.md` for full layout.

Planned entry: `index.php` → routes API requests for decks, slides, themes, auth.
Persistence: `data/slides.db` (SQLite).
Auth: `authentication/user`, `authentication/password`.
