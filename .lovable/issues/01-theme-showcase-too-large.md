# Issue — Theme/color showcase is too large

**Symptom:** The theme + color swatch picker renders too big; user wants a compact presentation.

**Repro:** Open controller → theme menu (palette icon). Swatch rows/labels take excessive space.

**Expected:** Compact, dense theme/color showcase (smaller swatches, tighter rows, scannable).

**Actual:** Oversized rows.

**Related files:** `src/slides/controls/ThemeMenu.tsx`.

**Status:** open
